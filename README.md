# hotspot-shell
This shell allows for the execution of remote commands when installed on the target system. The C&amp;C device uses it's capacity to create hotspots, thus the victim machine inteprets the Network Name/SSID as CMD commands on Windows.
